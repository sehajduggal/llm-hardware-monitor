I'll search the web for recent LLM optimization developments across the focus areas.

I have good coverage. Let me run a few more targeted searches on KV cache compression, MLX/Ollama updates, and exllamav2/EXL3.

Rate limited. Let me wait and retry.

I have enough material across all focus areas. Compiling the JSON findings.

I have comprehensive coverage. Here is the JSON output.

```json
{
  "findings": [
    {
      "claim": "Google's TurboQuant (PolarQuant rotation-based vector quantization, ICLR 2026) compresses KV cache from 16-bit to ~3-bit, cutting a 128K-token Llama 3 70B KV cache from ~40GB to under 7GB (6x reduction) with up to 8x faster attention on H100 and no loss on LongBench/Needle-in-a-Haystack.",
      "source": "research.google/blog/turboquant + nerdleveltech.com",
      "source_type": "official",
      "date": "June 18, 2026",
      "confidence_signal": "official_announcement",
      "performance_data": "6x KV cache memory reduction (40GB->7GB), 8x faster attention, full-precision accuracy retained",
      "related_domains": ["models", "hardware"]
    },
    {
      "claim": "DeepSeek-V4 uses hybrid compressed/sequence-length attention (CSA/HCA) that aggregates historical tokens into compressed memory blocks, making a 1M-token KV cache just ~10% of DeepSeek-V3.2 and ~2% of standard bf16 GQA (e.g. under ~7GB vs ~330GB for a Llama 3.1-70B equivalent), enabling 1M+ contexts on a single high-end GPU.",
      "source": "knightli.com DeepSeek-V4 KV cache analysis",
      "source_type": "blog",
      "date": "June 18, 2026",
      "confidence_signal": "multiple_reports",
      "performance_data": "KV cache reduced to ~2% of bf16 GQA baseline; 1M context on single GPU",
      "related_domains": ["models", "hardware"]
    },
    {
      "claim": "NVIDIA KVPress toolkit provides plug-and-play KV cache compressors for HuggingFace models, handling a 1M-token context on Llama 3.1-70B at single-digit GB VRAM versus >300GB for naive float16 cache.",
      "sou